{
    "name": "fp-extension",
    "version": "0.1.0",
    "private": true,
    "description": "Chrome extension port of the Foul Play Pokemon Showdown bot: reads the live battle, predicts the opponent's set, and searches for the best move with poke-engine compiled to WASM.",
    "license": "GPL-3.0-only",
    "repository": {
        "type": "git",
        "url": "https://github.com/pulkitahuja0/fp-extension"
    },
    "type": "module",
    "scripts": {
        "lint": "oxlint",
        "lint:fix": "oxlint --fix",
        "format": "oxfmt .",
        "format:check": "oxfmt --check .",
        "check": "npm run lint && npm run format:check",
        "build:wasm": "cd rs-wasm && ./build.sh",
        "package": "./scripts/package-extension.sh"
    },
    "devDependencies": {
        "oxfmt": "0.66.0",
        "oxlint": "1.81.0"
    }
}
